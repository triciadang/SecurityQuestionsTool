# SecurityQuestionsTool

question_answering.py allows you to change the parameters of the code (username, question list) without asking for user input.
QA_system.py is used for demonstration purposes.
